version: '3.8'

services:
  frontend:
    build: .
    container_name: frontend
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: http://localhost:3000
    networks:
      - indexer-network

networks:
  indexer-network:
    external: true
